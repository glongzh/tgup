# 统一以 LF 入库。Windows 上的编辑器很容易写入 CRLF，
# 而 gofmt 要求 LF —— 一旦某个文件带上 CRLF，CI 的 gofmt 检查就会红。
# 这里在 git 层面强制归一，任何平台 clone 下来都是 LF。
* text=auto eol=lf

# 二进制文件不做换行转换
*.exe   binary
*.jpg   binary
*.jpeg  binary
*.png   binary
*.mp4   binary

# 生成物不参与 diff 统计
go.sum  linguist-generated
